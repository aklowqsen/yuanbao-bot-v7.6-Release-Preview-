# 元宝Bot插件满血v7.6 Release Preview · 项目详细介绍

## 一、项目定位

「元宝Bot插件」是一款面向**元宝派 Bot** 的**全功能 Web 控制台插件**，用于把元宝派机器人接入后，在浏览器（桌面 / 手机）里集中管理消息、自动回复、素材、成员与 AI 对话等能力。本版本统一命名为 **元宝Bot插件满血v7.6 Release Preview**，定位为**创作者专属发行版**——由创建者本人分发，禁止商业用途与对外传播。

---

## 二、核心能力一览

### 1. 实时消息中心
- 通过 WebSocket 实时接收群聊消息，毫秒级推送、无轮询卡顿；
- 消息气泡按收发方左右分栏，进场淡入上滑动画；
- 顶部毛玻璃状态栏（连接状态 / 消息计数）+ 群聊筛选胶囊；
- 支持按关键词搜索过滤、图片点击放大（全功能版支持原图下载）、文件内联预览；
- 撤回消息后仍可预览 / 下载（已开放下载权限）。

### 2. 全模式发送
- 普通 / @艾特 / 刷屏 / 艾特刷屏 / 批量@ / 私聊 / 私聊刷屏 / LaTeX 消息 / LaTeX 刷屏 等 9 种模式；
- 底部悬浮吸底输入栏，聚焦光晕、自适应高度、回车即发；
- LaTeX 编辑器支持缩放、旋转、字体、边框等实时预览与批量生成。

### 3. 贴纸 / 成员管理
- 贴纸库搜索与一键发送；
- 群成员列表与本地用户管理。

### 4. 高级功能
- 引用回复、图片 / 文件 / 批量发送、艾特全体、AI 生图等。

### 5. AI 聊天
- 自定义大模型（OpenAI / 兼容接口）配置；
- 在线对话、文件 / 图片 / 生图，内置企业级科技主题。

### 6. 插件生态
- `plugins/` 目录扫描加载，支持插件商店、已安装管理与页面/卡片扩展。

### 7. 记录与撤回监听
- 本地消息记录写入 `logs/`；
- 撤回监听与撤回通知（可开关）。

### 8. Bot 转发（可选）
- 内置 Node 转发服务 `bot-forward/`，默认关闭，开启时弹安全警告。

---

## 三、界面与设计语言

- **统一设计系统（app-ui-v2）**：毛玻璃卡片、圆角、胶囊按钮、激活光晕、丝滑进场动效、灵动输入框，消息面板与所有 Tab 页视觉语言一致；
- **左侧圆形 Tab 栏（内部测试版专属布局）**：按钮图标圆形化、标签置于图标下方，压缩侧栏宽度，扩大消息区；
- **主题系统**：QQ 桌面端调色盘、暗/亮模式、背景模式、科技感 HUD 等；
- 响应式适配桌面与移动端（390×844 移动视口已验证）。

---

## 四、版本与会员体系

| 渠道 | 能力 |
|------|------|
| 内部测试版 `internal_beta` | 创建者自用全功能，内置真实凭据 |
| 全功能脱敏版 `free` | **对外分发版**：全部控制台能力开放（含图片/文件原图下载），凭据脱敏为占位符 |
| 高级版 / 旗舰版 | 全功能（含下载） |
| 体验版 / 初级版 / 中级版 | 历史渠道能力分级 |

---

## 五、技术架构

- **后端**：Python + Flask，单进程常驻，提供 REST API 与 WebSocket 消息流；
- **前端**：原生 HTML/CSS/JS（无构建步骤），Jinja2 模板渲染；
- **消息流**：SSE / WebSocket 毫秒级推送，前端增量渲染（rAF 合并），500 条消息渲染 < 30ms；
- **保活**：`start.sh` + `keepalive.sh`（PID 锁自愈，5 秒健康检查）；
- **部署形态**：可运行于服务器、树莓派、或 Android 手机 Termux。

---

## 六、已移除的能力（v7.0 变更）

| 功能 | 说明 |
|------|------|
| 厂商动态光影 | 原内部测试版专属的纯 CSS 厂商光影特效，已整体移除 |
| 在线更新 | 原「更新」Tab、123 网盘跳转下载、本地 `updates/` 自动抓取，已移除 |
| 一机一码验证 | 原基于机器指纹的 30 位验证密码在线认证，已移除 |

---

## 七、知识产权与合规

- 全部著作权归**创建者**所有；
- 仅限创建者本人分发；**禁止任何商业用途、禁止对外传播与二次分发、禁止逆向破解与去除版权声明**；
- 完整条款见 `LICENSE`；
- 「元宝」「元宝派」名称与标识归各自权利方，本软件为第三方定制控制台，与相关权利方无隶属关系。

---

## 八、获取与部署

- 解压分发包 → `pip install flask requests` → `python3 app_脱敏.py` → 浏览器打开 `http://localhost:5000/login`；
- 手机用户见 `TERMEX部署指南.md`；
- 脱敏版需先在 `config.json` 填入**你自己的**元宝派开放平台凭据方可连接。

© 2026 元宝Bot插件 · 创建者保留所有权利
